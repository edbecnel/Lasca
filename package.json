{
  "name": "lasca",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "start": "vite",
    "build": "vite build",
    "watch": "vite build --watch",
    "preview": "vite preview --port 8080",
    "deploy": "npm run build && gh-pages -d dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "jsdom": "^22.1.0",
    "typescript": "^5.6.0",
    "vite": "^5.0.0",
    "vitest": "^1.6.0"
  }
}
